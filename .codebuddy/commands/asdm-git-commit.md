首先检查当前对话上下文中是否存在先前生成的提交信息。 如果不存在，请提示用户先生使用 /git.生成符合规范的提交信息 成提交信息，并停止执行。 如果存在，再执行以下步骤:

执行 'git add -A' 命令暂存仓库中的所有更改。然后,使用上一个提示中生成的提交信息创建一个新的 git 提交,准确反映代码库中所做的修改。最后显示创建的提交摘要,包括提交哈希值、提交信息以及代码变更统计(例如:修改的文件数、新增行数、删除行数)。